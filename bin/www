#!/usr/bin/env node
const app = require('../index');
const logger = require('../logs/logger');

app.listen(3000, () => {
  // eslint-disable-next-line linebreak-style
  logger.info('启动服务器成功！监听3000端口');
});

